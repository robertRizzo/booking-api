spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bookingdb
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

  flyway:
    enabled: true
    baseline-on-migrate: true
    
management:
  endpoints:
    web:
      exposure:
        include: health,info

jwt:
  secret: nWE0FEfk9gRSSM1F6uATmVI9mW61RtssqTMgZiAdAwE=
  expiration: 86400000